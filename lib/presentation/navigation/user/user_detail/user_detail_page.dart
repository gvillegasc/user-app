import 'package:flutter/material.dart';

class UserDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          Center(child: Container(child: Text("Hola desde user detail Page")))
        ],
      ),
    );
  }
}
